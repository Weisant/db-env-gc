[project]
name = "db-env-gc"
version = "0.3.0"
description = "Plan-and-execute style database environment generator"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = []
