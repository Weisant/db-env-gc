[project]
name = "db-env-gc"
version = "1.0"
description = "database environment generator"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=2.30.0",
    "PyYAML>=6.0.0",
    "trafilatura>=1.8.0",
]

[tool.setuptools]
py-modules = []
